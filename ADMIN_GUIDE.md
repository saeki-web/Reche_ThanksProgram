# 管理者パネル ガイド

## 概要

更新された `tdc-ai-chat-widget.js` には、完全な問い合わせ管理システムが含まれています。

## 機能一覧

### 1. **管理者ボタン** (左下)
- チャットランチャーボタンと対称の位置（左下）に配置
- 茶色（#A8752E）で視認性を確保
- ホバー時に色が暗くなり、上に移動するアニメーション

### 2. **管理パネル**
管理者ボタンをクリックすると、右側に問い合わせ管理パネルが開きます。

#### **統計セクション**
パネルの上部に3つの主要メトリクスを表示：
- **総件数**: 登録された全問い合わせ数
- **未対応**: まだ対応していない問い合わせ数
- **対応済み**: すでに対応した問い合わせ数

#### **問い合わせ一覧表**
以下のカラムを含む表形式で表示：

| カラム | 説明 |
|--------|------|
| 日時 | 問い合わせ受付日時（YYYY/MM/DD HH:MM形式） |
| カテゴリ | 診療内容カテゴリ（虫歯・歯周病治療、ホワイトニング等） |
| 氏名 | 顧客のお名前 |
| 連絡先 | メールアドレスまたは電話番号 |
| 内容 | 問い合わせの概要テキスト |
| 状況 | ステータスボタン（未対応/対応済み） |

### 3. **ステータス管理**
- 各問い合わせの状況ボタンをクリックすると、「未対応」と「対応済み」を切り替え
- ボタンの色とクラスが自動更新
- 状態はブラウザの localStorage に保存

### 4. **キーボードショートカット**
- **Shift + A**: 管理パネルの開閉トグル

## データ保存について

### **ローカルストレージ**
- キー: `tdcw-inquiries`
- 問い合わせデータは自動的にブラウザの localStorage に保存されます
- ブラウザを閉じてもデータは保持されます（キャッシュをクリアするまで）

### **サーバー連携**（オプション）
CONFIG の INQUIRY_ENDPOINT を設定することで、サーバーに直接送信することも可能です：

```javascript
CONFIG.INQUIRY_ENDPOINT = "https://your-domain.com/api/inquiries"
```

この場合、問い合わせは localStorage に保存されるだけでなく、指定されたエンドポイントにもPOST送信されます。

## 問い合わせデータ構造

各問い合わせレコードは以下の構造を持ちます：

```javascript
{
  id: "mtekuq3fxtl9r",           // ユニークID
  ts: "2026/08/29 16:11",        // タイムスタンプ
  category: "虫歯・歯周病治療",  // カテゴリ
  name: "テスト太郎",            // 氏名
  contact: "test@example.com",   // 連絡先
  summary: "虫歯・歯周病治療のご予約希望", // 概要
  status: "未対応"               // ステータス（未対応/対応済み）
}
```

## 使用方法

### **基本的な操作**
1. **チャットを開く** → 右下のチャットアイコンをクリック
2. **問い合わせを送信** → チャットで診療希望やご質問を入力して送信
3. **管理パネルを開く** → 左下の管理者アイコンをクリック
4. **問い合わせを確認** → 表に一覧が表示されます
5. **ステータスを更新** → 状況ボタンをクリックして「未対応」/「対応済み」を切り替え

### **ショートカット**
- Shift + A で素早く管理パネルを開閉できます

## スタイリング

すべてのクラス名は `tdcw-` プレフィックスで統一されており、既存の WordPress テーマやプラグインとの CSS 衝突を防いでいます。

### **主要カラー**
- 管理者ボタン: #A8752E（茶色）
- 未対応ステータス: #FFE5CC（薄いオレンジ）
- 対応済みステータス: #D4EDDA（薄い緑）
- チャット（既存）: #178E86（ティール）

## 技術仕様

- **JavaScript**: Vanilla JS（フレームワーク不要）
- **データ保存**: localStorage API
- **ブラウザ互換性**: IE11以上（モダンブラウザ推奨）
- **ファイルサイズ**: 約33KB（minified版未対応）

## トラブルシューティング

### **問い合わせが表示されない**
- ブラウザの開発者ツール → コンソールで error を確認
- localStorage が有効か確認（プライベートモード等で無効の場合がある）

### **ステータス変更が保存されない**
- ブラウザのストレージ容量を確認
- localStorage をクリアしていないか確認

### **管理パネルが開かない**
- Shift + A または左下のボタンで開くことができます
- ブラウザの JavaScript が有効か確認

## 今後の拡張予定

- CSV エクスポート機能
- フィルタリング・ソート機能
- メール通知連携
- データ分析ダッシュボード
- 複数ユーザー対応
